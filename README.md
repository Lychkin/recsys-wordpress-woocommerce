# Рекомендательная система для e-commerce на WordPress и WooCommerce

## Описание проекта

Этот проект реализует **персонализированную рекомендательную систему** для интернет-магазина мебели на платформе **WordPress + WooCommerce**.

Система использует:

- историю покупок,
- просмотры товаров,
- добавления в корзину

для обучения **модели коллаборативной фильтрации**, которая рекомендует пользователям релевантные товары и похожие позиции.

Модель написана на **Python** и может интегрироваться в WordPress через **FastAPI**.

## Идея рекомендательной системы

Вместо явных оценок, используется **Implicit Feedback** — неявные сигналы интереса пользователя:

| Событие       | Смысл                        | Вес |
| ------------- | ---------------------------- | --- |
| `view`        | Пользователь посмотрел товар | 1   |
| `add_to_cart` | Проявил сильный интерес      | 3   |
| `purchase`    | Купил товар                  | 5   |

Эти сигналы агрегируются в **матрицу взаимодействий User × Item**, которая подаётся в модель.

## Используемый алгоритм

### Alternating Least Squares (ALS) for Implicit Feedback

Используется библиотека `implicit` (стандарт в e-commerce).

https://github.com/benfred/implicit

ALS:

- факторизует матрицу `User × Item`
- обучает латентные вектора пользователей и товаров
- позволяет находить:

  - персональные рекомендации
  - похожие товары

## Архитектура веб-приложения

```text
Routers Layer
    ↓
Service Layer
    ↓
ML Layer
    ↓
Files / DB / External APIs
```

## Алгоритм взаимодействия с системой

```
    WordPress + WooCommerce
      │               │
      │          JS + WP Hooks
      │               │
WooCommerce API  FastAPI (/event)
      │               │
      ├────────────────
      ▼
fetch.py → raw_events.csv
      │
      ▼
prepare_data.py → events.csv
      │
      ▼
train_als.py → als_model.pkl
      │
      ▼
FastAPI (/recommend/user/{user_id} & /recommend/item/{item_id})
      │
      ▼
WordPress recommendations
```

## Структура проекта

Cтруктура проекта разделяется на:

- API endpoints-слой, обработка запросов/ответов
- сервисный слой, бизнес-логика
- слой работы с ML-моделью
- слой сбора, подготовки и преобразования данных
- конфигурацию
- инфраструктурные компоненты

Детальное описание всех папок и файлов:

```text
│
├── app/
│   ├── core/                         # Ядро приложения и глобальная конфигурация
│   │   └── config.py                   # Загрузка настроек из .env, пути к файлам,
│   │
│   ├── routers/                      # HTTP-роутеры FastAPI
│   │   └── recommend.py                # Роутер с эндопоинтом получения рекомендаций FastAPI
│   │
│   ├── services/                     # Бизнес-логика приложения
│   │   └── recommendation_service.py   # Сервис получения рекомендаций. Связывает API и ML-слой
│   │
│   ├── ml/                           # Логика машинного обучения
│   │   ├── loader.py                   # Загрузка ALS-модели из файла. Используется при старте приложения
│   │   ├── train_als.py                # Обучение ALS-модели на уже подготовленных данных
│   │   ├── inference.py                # Генерация рекомендаций: рекомендации пользователю и похожие товары
│   │   └── exceptions.py
│   │
│   ├── etl/                          # ETL и взаимодействие с данными
│   │   ├── fetch.py                    # Сбор данных из внешних API (заказы, просмотры, события)
│   │   └── prepare_data.py             # Очистка и агрегация данных: веса событий, объединение взаимодействий, подготовка CSV для ALS
│   │
│   │── auth.py                       # Модуль авторизации (сверяет секретный ключ)
│   │── main.py                       # Точка входа FastAPI-приложения
│   └── __init__.py                   # Инициализация Python-пакета
│
├── data/                             # Данные появляющиеся в процессе функционирования системы
│   ├── raw_events.csv                  # "Сырые"" события из внешних API
│   ├── events.csv                      # Подготовленные взаимодействия пользователей
│   ├── popular_items.csv               # Популярные товары
│   └── als_model.pkl                   # Сохранённая ALS-модель
│
├── requirements.txt                    # Python-зависимости проекта
├── .env                                # Переменные окружения: API URL, токены
├── .flake8                             # Настройки линтера flake8
├── .gitignore                          # Исключения Git
└── README.md                           # Документация проекта
```

## API

| Endpoint                    | Назначение                |
| --------------------------- | ------------------------- |
| `/recommend/user/{user_id}` | персональные рекомендации |
| `/recommend/item/{item_id}` | похожие товары            |

Пример ответа:

```json
[
  { "item_id": "55", "score": 2.91 },
  { "item_id": "14", "score": 2.12 }
]
```

## Почему используется Implicit Feedback

В e-commerce:

- люди редко ставят оценки
- но много кликают, смотрят и кладут в корзину

Поэтому модель обучается на:

> «что пользователь делал», а не «что он сказал»

Это позволяет:

- рекомендовать даже без покупок
- работать с холодным стартом
- учитывать реальное поведение

## Как запустить

1. Установить зависимости:

```bash
pip install -r requirements.txt
```

2. Собрать данные:

```bash
python3 -m app.etl.fetch
```

3. Подготовить датасет:

```bash
python3 -m app.etl.prepare_data
```

4. Обучить модель:

```bash
python3 -m app.ml.train_als
```

5. Запустить API:

```bash
python3 -m app.main
```
